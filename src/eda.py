import logging

def run_eda(train_dataset, test_dataset):
    """
    Runs a minimal exploratory data analysis on the labels.
    :param train_dataset: The training dataset.
    :param test_dataset: The testing dataset.
    :return:
    """

    logger = logging.getLogger(__name__)

